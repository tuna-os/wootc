# Embedded entry point for wubildr.efi. The external configuration stays on
# the Windows NTFS partition so Windows can update image/host parameters.
search -s -f -n /wootc/install/wubildr.cfg
configfile /wootc/install/wubildr.cfg
