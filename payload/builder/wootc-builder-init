#!/bin/sh
# A helper VM owns only two dedicated image files exported as VirtIO disks.
# Never discover or mount a Windows host volume from this environment.
set -eu
. /lib/wootc-builder.sh
builder_main
